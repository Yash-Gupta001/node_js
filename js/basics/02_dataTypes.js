"use strict";

// alert(3+3) /// we are using nodejs not browser

/*
In JavaScript, semicolons are technically optional most of the time because of a feature called
Automatic Semicolon Insertion (ASI).
That means JavaScript automatically adds semicolons where it thinks they’re needed
*/

console.log(3 + 3);

let name = "yash";
let age = 23;
let isLoggedIn = false;


/// type 
/*
primitive datatypes

number => 2 to power 53
bigint
string
boolean
null => it is a datatype and also a standalone value. and it is object also
undefined => 
symbol => unique 
*/

/// object



/*
| Operator | Meaning                                             |
| -------- | --------------------------------------------------- |
| `=`      | Assignment (store value in a variable)              |
| `==`     | Equality (checks value only, type coercion allowed) |
| `===`    | Strict equality (checks value **and type**)         |
| `!=`     | Not equal (value only)                              |
| `!==`    | Strict not equal (value **and type**)               |
*/